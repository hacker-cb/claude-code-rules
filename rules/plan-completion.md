# How a plan ends

Every plan written in plan mode closes with a section saying how the work
completes: through `hcb-dev:shipping-workflow` in `request` mode — the reviewers,
their fixes, the coverage gate, then a change request driven until it is
mergeable — and **stopping there, ready to merge**. The merge itself waits for
the user's word.
